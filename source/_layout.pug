<!-- The (_layout.pug, _layout.scss) files define
layout restrictions for each individual section (grid area) -->

// Preprocessor Mixins
include _mixins
// Header
include partials/header

// Main Layout
main.container-fluid.pt-4
  // Top Story
  section(style={"grid-area": "top-story"})
    include partials/top-story

  // More Stories
  section(style={"grid-area": "more-stories"})
    include partials/more-stories

  // Culture & Travel
  section(style={"grid-area": "culture-travel"})
    include partials/culture-travel

  // Featured Stories
  section(style={"grid-area": "featured-stories"})
    include partials/featured-stories

  // Opinion
  section(style={"grid-area": "opinion"})
    include partials/opinion

  // Latest News
  section(style={"grid-area": "latest-news"})
    include partials/latest-news

  // TODO: Does Not Exist
  section(style={"grid-area": ""})
    // Sponsored Insight
    // Sign Up
    // Sponsored Insight * 4
    // Monthly Issue

  // In The Magazine
  section(style={"grid-area": "in-the-magazine"})
    include partials/in-the-magazine

  // Newsweek Conversations
  section(style={"grid-area": "newsweek-conversations"})
    include partials/newsweek-conversations

  // Editors Pick
  section(style={"grid-area": "editors-pick"})
    include partials/editors-pick

  // Featured Slideshows
  section(style={"grid-area": "featured-slideshows"})
    include partials/featured-slideshows

  // TODO: Does Not Exist
  section(style={"grid-area": ""})
    // U.S.
    // World
    // Business
    // Tech & Science
    // Culture
    // Sports

  // TODO: Does Not Exist
  section(style={"grid-area": ""})
    // Subscribe
    // Newsletter

// Footer
include partials/footer
